{
  "name": "rbw-booking-widget",
  "version": "1.0.0",
  "scripts": {
    "build": "cp widget.js backend/widget.js && cd backend && npm ci",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
